#include "main.h"

/**
 * print_most_numbers - prints 0 - 9 except 2, 3
 * Description: prints
 * Return: void
 */

void print_most_numbers(void)
{
	int x = 0;

	while (x <= 9)
	{
		if (x != 2 && a != 4)
		{
			_putchar(x + ');
		}
		x++;
	}
	_putchar('\n');
}
